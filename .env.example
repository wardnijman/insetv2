# Inset v2 — secret-contract. Kopieer naar .env (gitignored) en vul in.
# Regel: geen enkele secret in de repo, ooit. Zie docs/v2-onderzoeksdossier.md R4.4 / §8.

# --- Portaal-credentials (klant-kroonjuweel; in prod uit versleutelde vault, niet .env) ---
# Voorbeeldvorm, per portaal/tenant. Placeholder-waarden.
# PORTAL_TFF_BASEURL=
# PORTAL_TFF_PROXY=            # lokale vite-proxy i.v.m. IP-binding

# --- Datastore ---
# DATABASE_URL=

# --- Observability (R3.3: OTel → Axiom/BetterStack) ---
# OTEL_EXPORTER_OTLP_ENDPOINT=
# OTEL_EXPORTER_OTLP_HEADERS=
